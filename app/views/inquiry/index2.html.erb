<!DOCTYPE html>
<html>
          <head>
            <title>Co-Step</title>
            <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
            <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
            <%= csrf_meta_tags %>
          </head>

  <body>
      <nav>
        <div id="hideBox">
        <div id="logo-picture"></div>
          <p class="nButton" id="pageTop" ><%= link_to "Home", co_steps_path %></p>
        </div>
      </nav>

    <div class="body_alert">
     
        
      <div class="alert-danger_recruit" role="alert">
          <%= form_for @inquiry, :url => inquiry_confirm2_path do |f| %>
                <% if @inquiry.errors.any? %>
                      <strong>入力内容にエラーがあります</strong>
                      <ul>
                        <% @inquiry.errors.each do |attr, msg| %>
                          <li><%= msg %></li>
                        <% end %>
                        
                      </ul>
                <% end %>
           
            <table class="table">
                  <tr>
                    <th>名前<span class="text-danger">（必須）</span></th>
                    <td><%= f.text_field :name %></td>
                  </tr>
                  <tr>
                    <th>メールアドレス<span class="text-danger">（必須）</span></th>
                    <td><%= f.text_field :email %></td>
                  </tr>
                  <tr>
                    <th>志望理由</th>
                    <td><%= f.text_area :message %></td>
                  </tr>
                  <tr>
                    <th>Select File
                    <td> <%= f.file_field :file %></td>
                    </th>
                  </tr>
                  <tr>
                    <th>送信</th>
                    <td><div class="button"><%= f.submit '確認', class: 'btn-btn-primary' %></div></td>
                  </tr>
            </table>
     <% end %>
                  </div>
      </div>
  </body>
</html>